version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8011:8011"
    volumes:
      - ../models:/ML_project/models
    networks:
      - ml_network

networks:
  ml_network:
    external:
      name: ml_app_network